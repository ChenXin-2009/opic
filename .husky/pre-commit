#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 运行 linter
echo "🔍 运行 ESLint 检查..."
npm run lint

# 运行 TypeScript 类型检查
echo "🔍 运行 TypeScript 类型检查..."
npx tsc --noEmit

# 运行测试
echo "🧪 运行测试..."
npm test -- --passWithNoTests

echo "✅ 预提交检查通过！"
